# Dependências Python — Canal Cortes Automação

# Download de vídeos do YouTube + extração de heatmap
yt-dlp>=2024.1.0

# IA — LLM (OpenRouter) e transcrição leve (faster-whisper)
openai>=1.14.0
faster-whisper>=1.0.0

# Detecção de rosto (face tracking)
mediapipe==0.10.14

# Processamento de vídeo frame a frame
opencv-python-headless>=4.8.0

# HTTP — downloads e APIs de imagem
requests>=2.31.0

# Busca de imagens via DuckDuckGo (Estratégia 1: Bing CDN HD)
duckduckgo-search>=6.0.0

# Imagens — overlay e composição
Pillow>=10.0.0

# Arrays numéricos para face tracking
numpy>=1.24.0

# YouTube Data API v3
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
